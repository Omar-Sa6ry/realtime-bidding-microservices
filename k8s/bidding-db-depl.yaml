apiVersion: apps/v1
kind: Deployment
metadata:
  name: bidding-db-depl
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: bidding-db
  template:
    metadata:
      labels:
        app: bidding-db
    spec:
      containers:
        - name: bidding-db
          image: mongo:6.0
          env:
            - name: MONGO_INITDB_ROOT_USERNAME
              valueFrom:
                secretKeyRef:
                  name: bidding-secrets
                  key: MONGO_INITDB_ROOT_USERNAME
            - name: MONGO_INITDB_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: bidding-secrets
                  key: MONGO_INITDB_ROOT_PASSWORD
            - name: MONGO_INITDB_DATABASE
              value: bidding-bidding_service_dev
          ports:
            - containerPort: 27017
          volumeMounts:
            - name: mongo-storage
              mountPath: /data/db
      volumes:
        - name: mongo-storage
          persistentVolumeClaim:
            claimName: bidding-db-pvc
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bidding-db-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: bidding-db-srv
spec:
  selector:
    app: bidding-db
  ports:
    - name: mongodb
      protocol: TCP
      port: 27017
      targetPort: 27017
