apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-db-depl
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: ai-db
  template:
    metadata:
      labels:
        app: ai-db
    spec:
      containers:
        - name: ai-db
          image: mongo:6.0
          env:
            - name: MONGO_INITDB_ROOT_USERNAME
              valueFrom:
                secretKeyRef:
                  name: ai-secrets
                  key: MONGO_INITDB_ROOT_USERNAME
            - name: MONGO_INITDB_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: ai-secrets
                  key: MONGO_INITDB_ROOT_PASSWORD
            - name: MONGO_INITDB_DATABASE
              value: bidding-ai_service_dev
          ports:
            - containerPort: 27017
          volumeMounts:
            - name: mongo-storage
              mountPath: /data/db
      volumes:
        - name: mongo-storage
          persistentVolumeClaim:
            claimName: ai-db-pvc
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ai-db-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard
---
apiVersion: v1
kind: Service
metadata:
  name: ai-db-srv
spec:
  selector:
    app: ai-db
  ports:
    - name: mongodb
      protocol: TCP
      port: 27017
      targetPort: 27017
